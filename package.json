{
  "name": "node-fetch-mirror",
  "version": "1.0.0",
  "description": "Forward every request to an origin host and relay the exact headers back to the client",
  "type": "module",
  "engines": { "node": ">=18" },
  "scripts": {
    "start": "node index.js"
  },
  "dependencies": {
    "undici": "^6.17.0"
  }
}
